**What does this change, and why?**

**Checklist**
- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run verify:app` passes on my platform
- [ ] New pure logic has a `.test.ts` beside it; UI strings are in
      `src/shared/ui-strings.ts` (both locales); design values go through
      tokens (see CONTRIBUTING.md)
